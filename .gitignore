# Runtime state for the ops role, created inside whatever repository
# ops-cycle is installed into — never inside this repository itself, but
# ignored here too in case a smoke test is ever run from this checkout.
ops/state.md
