# Postmortem: <incident title> (<incident-id>)

> Blameless. Assume good faith of all participants. This document exists to
> improve the system, not to assign blame to a person.

## Impact

- What broke:
- For whom (users/services affected):
- How long (start / detected / mitigated / resolved timestamps):

## Actions taken during response

(Drawn from the live incident timeline — timestamp, event/observation,
actor, and for decision points, what was decided and by whom.)

-

## Root cause(s)

-

## Prevention / follow-up action items

Each item requires all three fields non-empty before it counts as done:

| action item | owner (one named individual, not a team) | tracking location (outside this doc) | closing/verification condition |
|---|---|---|---|
| | | | |

## Review

- Reviewed by (named human, not "the team"):
- Reviewer satisfied with document and action items: yes / no
- Date reviewed:

(This section is filled in by a human reviewer, not self-certified by the
agent that wrote the draft above. `incident -> steady` in
transition-rules.md requires this review to have actually happened, stated
by the user in their own turn — a filled-in "yes" here is evidence for that
conversation, not a substitute for it.)
