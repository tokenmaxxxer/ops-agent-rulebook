ROLLOUT-PLAN (phase 2, ops/rollout-plan.md, per step): every metric inside a step must carry a non-empty threshold declared before that step is marked result: pass or result: fail — never invent or adjust a threshold in the moment a result comes in. result: pending is never gated; a step held pending needs no threshold yet. rollout-plan-fields-gate.sh refuses a write that resolves a step to pass/fail with any metric's threshold unset or blank; it names the offending step and metric in its refusal.
