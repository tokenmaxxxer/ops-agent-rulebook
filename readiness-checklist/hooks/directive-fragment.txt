READINESS-CHECKLIST (phase 2, ops/state.md status: readiness -> rollout): every one of the seven PRR dimensions (service levels, architecture review, performance, documentation, observability, testing, deployment strategy) must resolve to an explicit yes/no line in the `## Checklist` section, and every yes must carry a non-empty `artifact:` pointer a stranger could actually open (dashboard URL, runbook path, config key, repo file) before status may flip to rollout. "We have monitoring" with nothing to link is a FAIL, not a pass with a caveat — mark it `no` and keep working it rather than writing a yes with an empty artifact field. readiness-fields-gate.sh refuses the status: rollout write outright when any item is unresolved or unpointed; it does not gate any other write to ops/state.md.
